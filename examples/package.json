{
  "name": "examples",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "main": "index.js",
  "scripts": {
    "build": "browserify -d -e app.js -o bundle.js -t babelify",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "azu",
  "license": "MIT",
  "dependencies": {
    "es6-symbol": "^2.0.1",
    "material-flux": "file:..",
    "react": "^0.13"
  },
  "devDependencies": {
    "babel": "^4.7.8",
    "babelify": "^5.0.4",
    "browserify": "^9.0.3"
  }
}
